import 'package:flutter/material.dart';

class WritingInquiries extends StatelessWidget {
  const WritingInquiries({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
