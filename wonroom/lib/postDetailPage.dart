import 'package:flutter/material.dart';

class Postdetailpage extends StatelessWidget {  // Rename constructor to `Postdetailpage`
  final Map<String, dynamic> post;

  const Postdetailpage({Key? key, required this.post}) : super(key: key);  // Constructor matches the class name

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
